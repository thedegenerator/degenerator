import { calcPercent } from 'degenerator-ui/helpers/calc-percent';
import { module, test } from 'qunit';

module('Unit | Helper | calc percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcPercent([42]);
  assert.ok(result);
});
